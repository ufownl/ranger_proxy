AUX_SOURCE_DIRECTORY(. PROXY_SOURCES)
ADD_EXECUTABLE(ranger_proxy ${PROXY_SOURCES})
TARGET_LINK_LIBRARIES(ranger_proxy ${CAF_LIBRARIES} ${OPENSSL_CRYPTO_LIBRARY})

INSTALL(TARGETS ranger_proxy DESTINATION bin)
